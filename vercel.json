{
  "version": 2,
  "installCommand": "npm install --production",
  "buildCommand": "echo 'Skipping build - using committed files'",
  "builds": [
    {
      "src": "server/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/server.js"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/server/server.js"
    }
  ]
}

